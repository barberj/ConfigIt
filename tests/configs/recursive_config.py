config_dict = {
    'name': 'recursive',
}

some_other_dict = dict(
    foo='bar',
    answer=42
)

recursive_test = dict(
    first=dict(
        second=dict(
            last=3.14
        )
    )
)
